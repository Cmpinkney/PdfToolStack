@page "/redact-pdf-online"

<PageTitle>Redact PDF Online Securely — Hide Sensitive Information | PdfToolStack</PageTitle>
<HeadContent>
    <meta name="description" content="Redact PDF online securely. Permanently cover names, emails, account numbers, and sensitive details before sharing your PDF. No installs, no signup required." />
    <link rel="canonical" href="https://pdftoolstack.com/redact-pdf" />
</HeadContent>

<div class="lp-page redact-lp">
    <section class="lp-hero redact-lp-hero">
        <div class="lp-hero-inner">
            <div class="lp-badge redact-lp-badge">Secure Redaction</div>
            <h1 class="lp-h1">Redact PDF online <span class="redact-lp-accent">securely</span></h1>
            <p class="lp-sub">
                Permanently cover sensitive information before sharing your PDF. No installs, no signup required.
            </p>
            <div class="lp-cta-row">
                <a href="/redact-pdf" class="lp-btn-primary redact-lp-btn">
                    Redact a PDF now →
                </a>
                <a href="#redact-steps" class="redact-lp-secondary">See how it works</a>
            </div>
            <p class="lp-trust-inline redact-lp-trust">Private processing • Secure upload • Auto-delete after 1 hour</p>
        </div>

        <div class="redact-lp-visual" aria-hidden="true">
            <div class="redact-lp-doc">
                <div class="redact-lp-doc-top">
                    <span>Client_Record.pdf</span>
                    <span>Preview</span>
                </div>
                <div class="redact-lp-doc-body">
                    <div class="redact-lp-line redact-lp-line-long"></div>
                    <div class="redact-lp-line"></div>
                    <div class="redact-lp-row">
                        <span>Name</span>
                        <strong class="redact-lp-mark redact-lp-mark-one"></strong>
                    </div>
                    <div class="redact-lp-row">
                        <span>Email</span>
                        <strong class="redact-lp-mark redact-lp-mark-two"></strong>
                    </div>
                    <div class="redact-lp-row">
                        <span>Account</span>
                        <strong class="redact-lp-mark redact-lp-mark-three"></strong>
                    </div>
                    <div class="redact-lp-line redact-lp-line-long"></div>
                    <div class="redact-lp-line redact-lp-line-short"></div>
                </div>
                <div class="redact-lp-safe-badge">Ready to share</div>
            </div>
        </div>
    </section>

    <section class="lp-section lp-pain-section">
        <div class="lp-section-inner">
            <h2 class="lp-h2">Stop sensitive information from leaving your hands</h2>
            <div class="lp-cards-3">
                <div class="lp-card">
                    <div class="lp-card-icon">ID</div>
                    <h3>Hide personal details</h3>
                    <p>Cover names, emails, phone numbers, addresses, IDs, and other private fields before sending a file.</p>
                </div>
                <div class="lp-card">
                    <div class="lp-card-icon">CL</div>
                    <h3>Protect client information</h3>
                    <p>Prepare records, statements, contracts, and case files without exposing confidential details.</p>
                </div>
                <div class="lp-card">
                    <div class="lp-card-icon">SH</div>
                    <h3>Prepare documents for sharing</h3>
                    <p>Make PDFs safer for email, review, public requests, vendor handoffs, or internal approvals.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="lp-section lp-how-section" id="redact-steps">
        <div class="lp-section-inner">
            <div class="lp-label">How it works</div>
            <h2 class="lp-h2">Redact before you share</h2>
            <div class="redact-lp-steps">
                <div class="lp-step"><div class="lp-step-num redact-lp-num">1</div><h3>Upload your PDF</h3><p>Select the document that contains sensitive information.</p></div>
                <div class="lp-step"><div class="lp-step-num redact-lp-num">2</div><h3>Mark sensitive areas</h3><p>Cover names, emails, account numbers, signatures, or any private region.</p></div>
                <div class="lp-step"><div class="lp-step-num redact-lp-num">3</div><h3>Review redactions</h3><p>Check the marked areas before creating the final file.</p></div>
                <div class="lp-step"><div class="lp-step-num redact-lp-num">4</div><h3>Download the finished file</h3><p>Save the redacted PDF and share it with more confidence.</p></div>
            </div>
            <div class="lp-center-cta"><a href="/redact-pdf" class="lp-btn-primary redact-lp-btn">Start processing</a></div>
        </div>
    </section>

    <section class="lp-section redact-lp-use-section">
        <div class="lp-section-inner">
            <div class="lp-label">Common use cases</div>
            <h2 class="lp-h2">Built for sensitive documents</h2>
            <div class="redact-lp-use-grid">
                <div>Legal documents</div>
                <div>HR paperwork</div>
                <div>Financial statements</div>
                <div>Client records</div>
                <div>Public records requests</div>
            </div>
        </div>
    </section>

    <section class="lp-section redact-lp-security">
        <div class="lp-section-inner redact-lp-security-inner">
            <div>
                <div class="lp-label">Trust and security</div>
                <h2 class="lp-h2">Designed for files you would rather not expose</h2>
                <p>
                    Upload your PDF, mark what should not be shared, and download a finished copy. Files are processed securely and auto-deleted after processing, so your documents are not kept around permanently.
                </p>
            </div>
            <div class="redact-lp-security-list">
                <div><strong>Secure processing</strong><span>Built for private document workflows.</span></div>
                <div><strong>Auto-delete</strong><span>Uploaded files are removed after a short retention window.</span></div>
                <div><strong>Permanent redaction intent</strong><span>Create a copy meant to hide sensitive details before sharing.</span></div>
            </div>
        </div>
    </section>

    <section class="lp-section redact-lp-faq">
        <div class="lp-section-inner">
            <div class="lp-label">FAQ</div>
            <h2 class="lp-h2">Questions about online PDF redaction</h2>
            <div class="redact-lp-faq-grid">
                <div class="redact-lp-faq-item">
                    <h3>Can I redact a PDF online for free?</h3>
                    <p>Yes. You can open the Redact PDF tool, upload your document, mark sensitive areas, and download the result.</p>
                </div>
                <div class="redact-lp-faq-item">
                    <h3>Is online PDF redaction secure?</h3>
                    <p>PdfToolStack is designed around private processing, secure upload, and automatic file deletion after processing.</p>
                </div>
                <div class="redact-lp-faq-item">
                    <h3>Are redactions permanent?</h3>
                    <p>The tool is intended to create a redacted output file before sharing. Always review the downloaded PDF before sending it externally.</p>
                </div>
                <div class="redact-lp-faq-item">
                    <h3>Can I remove names, emails, or account numbers?</h3>
                    <p>Yes. You can mark any visible area, including names, emails, addresses, account numbers, IDs, and signatures.</p>
                </div>
                <div class="redact-lp-faq-item">
                    <h3>Do I need to install software?</h3>
                    <p>No. The workflow runs in your browser, so there is no desktop app or extension to install.</p>
                </div>
                <div class="redact-lp-faq-item">
                    <h3>What happens to my uploaded file?</h3>
                    <p>Your file is processed for the redaction workflow and automatically deleted after the retention window.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="lp-final-cta-section">
        <div class="lp-final-inner">
            <h2>Ready to redact your PDF?</h2>
            <p>Cover sensitive information before sharing your document.</p>
            <a href="/redact-pdf" class="lp-btn-primary redact-lp-btn lp-btn-lg">Open Redact PDF tool</a>
            <p class="lp-fine">Private processing • Secure upload • Files auto-deleted</p>
        </div>
    </section>
</div>

<style>
    .redact-lp .lp-h1 {
        max-width: 760px;
    }

    .redact-lp-hero {
        background:
            radial-gradient(circle at top right, rgba(109, 93, 252, 0.18), transparent 34%),
            linear-gradient(135deg, #f8f7ff 0%, #eef5ff 100%);
    }

    .redact-lp-badge {
        background: rgba(109, 93, 252, 0.12);
        color: #4f46e5;
        border: 1px solid rgba(109, 93, 252, 0.22);
    }

    .redact-lp-accent {
        background: linear-gradient(135deg, #6d5dfc 0%, #2563eb 55%, #06b6d4 100%);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    .redact-lp-btn {
        background: linear-gradient(135deg, #6d5dfc 0%, #2563eb 55%, #06b6d4 100%);
        box-shadow: 0 16px 34px rgba(37, 99, 235, 0.22);
    }

    .redact-lp-btn:hover {
        box-shadow: 0 20px 44px rgba(37, 99, 235, 0.32);
    }

    .redact-lp-secondary {
        color: #4f46e5;
        font-weight: 800;
        text-decoration: none;
    }

    .redact-lp-secondary:hover {
        color: #2563eb;
    }

    .redact-lp-trust {
        display: block;
        margin-top: 0.7rem;
    }

    .redact-lp-visual {
        position: relative;
        min-height: 390px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .redact-lp-doc {
        position: relative;
        width: min(380px, 92%);
        padding: 22px;
        border: 1px solid rgba(148, 163, 184, 0.22);
        border-radius: 28px;
        background: rgba(255, 255, 255, 0.92);
        box-shadow: 0 28px 70px rgba(15, 23, 42, 0.14);
        overflow: hidden;
    }

    .redact-lp-doc::before {
        content: "";
        position: absolute;
        inset: auto -24px -44px auto;
        width: 170px;
        height: 170px;
        border-radius: 50%;
        background: rgba(37, 99, 235, 0.12);
    }

    .redact-lp-doc-top {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        color: #475569;
        font-size: 0.78rem;
        font-weight: 800;
        margin-bottom: 18px;
    }

    .redact-lp-doc-body {
        position: relative;
        padding: 18px;
        border-radius: 18px;
        background: #f8fafc;
        border: 1px solid #e2e8f0;
    }

    .redact-lp-line {
        width: 72%;
        height: 8px;
        border-radius: 999px;
        background: #cbd5e1;
        margin-bottom: 14px;
    }

    .redact-lp-line-long {
        width: 92%;
    }

    .redact-lp-line-short {
        width: 46%;
    }

    .redact-lp-row {
        display: grid;
        grid-template-columns: 82px 1fr;
        gap: 14px;
        align-items: center;
        margin: 14px 0;
        color: #64748b;
        font-size: 0.82rem;
        font-weight: 800;
    }

    .redact-lp-mark {
        display: block;
        height: 16px;
        border-radius: 4px;
        background: #0f172a;
        box-shadow: 0 0 0 4px rgba(15, 23, 42, 0.04);
        animation: redactSweep 3.8s ease-in-out infinite;
    }

    .redact-lp-mark-two {
        width: 78%;
        animation-delay: 0.35s;
    }

    .redact-lp-mark-three {
        width: 58%;
        animation-delay: 0.7s;
    }

    .redact-lp-safe-badge {
        position: relative;
        z-index: 1;
        display: inline-flex;
        margin-top: 18px;
        padding: 9px 13px;
        border-radius: 999px;
        background: #ecfeff;
        color: #0369a1;
        border: 1px solid #bae6fd;
        font-size: 0.78rem;
        font-weight: 850;
    }

    .redact-lp-steps {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 1rem;
    }

    .redact-lp-num {
        background: linear-gradient(135deg, #6d5dfc 0%, #2563eb 100%);
    }

    .redact-lp-use-grid,
    .redact-lp-faq-grid {
        display: grid;
        gap: 1rem;
    }

    .redact-lp-use-grid {
        grid-template-columns: repeat(5, minmax(0, 1fr));
    }

    .redact-lp-use-grid div,
    .redact-lp-faq-item,
    .redact-lp-security-list div {
        border: 1px solid rgba(148, 163, 184, 0.18);
        border-radius: 18px;
        background: rgba(255, 255, 255, 0.9);
        box-shadow: 0 14px 34px rgba(15, 23, 42, 0.06);
    }

    .redact-lp-use-grid div {
        padding: 1rem;
        color: #1e293b;
        font-weight: 850;
        text-align: center;
    }

    .redact-lp-security-inner {
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(320px, 0.8fr);
        gap: 2rem;
        align-items: start;
    }

    .redact-lp-security-inner p {
        max-width: 690px;
        color: #64748b;
        line-height: 1.75;
        font-size: 1rem;
    }

    .redact-lp-security-list {
        display: grid;
        gap: 1rem;
    }

    .redact-lp-security-list div {
        padding: 1rem;
    }

    .redact-lp-security-list strong,
    .redact-lp-security-list span {
        display: block;
    }

    .redact-lp-security-list strong {
        color: #0f172a;
        font-size: 0.95rem;
        margin-bottom: 0.25rem;
    }

    .redact-lp-security-list span {
        color: #64748b;
        line-height: 1.55;
        font-size: 0.88rem;
    }

    .redact-lp-faq-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .redact-lp-faq-item {
        padding: 1.15rem;
    }

    .redact-lp-faq-item h3 {
        margin: 0 0 0.45rem;
        color: #0f172a;
        font-size: 1rem;
    }

    .redact-lp-faq-item p {
        margin: 0;
        color: #64748b;
        line-height: 1.65;
        font-size: 0.92rem;
    }

    @@keyframes redactSweep {
        0%, 100% {
            opacity: 0.86;
            transform: scaleX(0.94);
            transform-origin: left center;
        }

        48% {
            opacity: 1;
            transform: scaleX(1);
        }
    }

    @@media (max-width: 980px) {
        .redact-lp-steps,
        .redact-lp-use-grid,
        .redact-lp-security-inner,
        .redact-lp-faq-grid {
            grid-template-columns: 1fr;
        }

        .redact-lp-visual {
            min-height: 320px;
        }
    }
</style>
